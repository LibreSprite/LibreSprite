name: MacOS

on:
  push:
    branches: [ master ]
    tags:
      - '*'
  pull_request:
    branches: [ master ]

permissions:
  contents: write

env:
  BUILD_TYPE: Release

jobs:
  build-arm64:
    runs-on: macos-latest
    if: ${{ !contains(github.event.head_commit.message, 'NO_SW_CHANGE') && !contains(github.event.pull_request.body, 'NO_SW_CHANGE') }}
    steps:
    - uses: actions/checkout@v7
      with:
        submodules: 'true'
    - name: Check for git tag
      id: check_tag
      run: |
        TAG=$(git describe --exact-match --tags 2>/dev/null) || true
        if [ -n "$TAG" ]; then
          echo "has_tag=true" >> $GITHUB_OUTPUT
          echo "tag=$TAG" >> $GITHUB_OUTPUT
          echo "version=${TAG#v}" >> $GITHUB_OUTPUT
        else
          echo "has_tag=false" >> $GITHUB_OUTPUT
        fi
    - name: Get short commit hash
      id: commit
      run: |
        SHORT_HASH=$(git rev-parse --short HEAD)
        echo "short_hash=$SHORT_HASH" >> $GITHUB_OUTPUT
    - name: Prepare release version
      run: |
        sed -i '' "s/\"local build\"/\"${{ steps.commit.outputs.short_hash }}\"/g" src/config.h
        if [ "${{ steps.check_tag.outputs.has_tag }}" = "true" ]; then
          VER="${{ steps.check_tag.outputs.version }}"
          /usr/libexec/PlistBuddy -c "Set :CFBundleVersion $VER" packaging/macos/Info.plist
          /usr/libexec/PlistBuddy -c "Set :CFBundleShortVersionString $VER" packaging/macos/Info.plist
          /usr/libexec/PlistBuddy -c "Set :CFBundleLongVersionString $VER" packaging/macos/Info.plist
        fi
    - name: Prepare environment and install dependencies
      run: |
        # macos-latest runner images come with this tap pre-added; Homebrew's
        # tap-trust check then warns on every brew invocation since it's untrusted.
        brew untap aws/tap || true
        brew install autoconf automake libtool gnutls freetype jpeg webp pixman sdl2 sdl2_image tinyxml2 libarchive ninja zlib xmlto dylibbundler
        pip3 install --break-system-packages dmgbuild
        echo "Building and Installing GifLib"
        export CFLAGS="-Wno-implicit-function-declaration"
        curl -L https://sourceforge.net/projects/giflib/files/giflib-4.x/giflib-4.2.3.tar.gz/download --output giflib.tar.gz
        tar -xvf giflib.tar.gz
        cd giflib-4.2.3/
        ./configure
        sudo make install -j4
        cd ..
        echo "Building and installing LibPNG"
        git clone https://github.com/glennrp/libpng -b v1.6.50 --recursive
        cd libpng/
        ./configure
        make all -j4
        sudo make install
        cd ..
        plutil packaging/macos/Info.plist
    - name: Generate Makefiles
      run: |
        mkdir build
        if [ "${{ steps.check_tag.outputs.has_tag }}" = "true" ]; then
          cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release \
            -DRELEASE_TAG=ON \
            -DRELEASE_VERSION="${{ steps.check_tag.outputs.version }}" \
            -DCMAKE_OSX_ARCHITECTURES=arm64 \
            -DCMAKE_OSX_DEPLOYMENT_TARGET=13.3 \
            -DCMAKE_OSX_SYSROOT=/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk
        else
          cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_OSX_ARCHITECTURES=arm64 \
            -DCMAKE_OSX_DEPLOYMENT_TARGET=13.3 \
            -DCMAKE_OSX_SYSROOT=/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk
        fi
    - name: Build the binaries
      run: |
        # Build only the editor target so test/tool binaries (clip_*_tests,
        # gen) are never produced and can't leak into the bundle.
        cmake --build build -j 3 --target libresprite
        rm -f build/bin/gen
    - name: Generate .icns file
      run: |
        mkdir libresprite.iconset
        sips -z 16 16     data/icons/ase64.png --out libresprite.iconset/icon_16x16.png
        sips -z 32 32     data/icons/ase64.png --out libresprite.iconset/icon_16x16@2x.png
        sips -z 32 32     data/icons/ase64.png --out libresprite.iconset/icon_32x32.png
        sips -z 64 64     data/icons/ase64.png --out libresprite.iconset/icon_32x32@2x.png
        sips -z 128 128   data/icons/ase64.png --out libresprite.iconset/icon_128x128.png
        sips -z 256 256   data/icons/ase64.png --out libresprite.iconset/icon_128x128@2x.png
        sips -z 256 256   data/icons/ase64.png --out libresprite.iconset/icon_256x256.png
        sips -z 512 512   data/icons/ase64.png --out libresprite.iconset/icon_256x256@2x.png
        sips -z 512 512   data/icons/ase64.png --out libresprite.iconset/icon_512x512.png
        sips -z 1024 1024 data/icons/ase64.png --out libresprite.iconset/icon_512x512@2x.png
        iconutil -c icns -o libresprite.icns libresprite.iconset
        rm -R libresprite.iconset
    - name: Create Apple Bundle
      run: |
        mkdir bundle/
        mkdir bundle/libresprite.app
        mkdir bundle/libresprite.app/Contents
        mkdir bundle/libresprite.app/Contents/libs
        mkdir bundle/libresprite.app/Contents/MacOS
        mkdir bundle/libresprite.app/Contents/Resources
        cp build/bin/libresprite bundle/libresprite.app/Contents/MacOS/
        cp -r build/bin/data/ bundle/libresprite.app/Contents/Resources/data/
        cp libresprite.icns bundle/libresprite.app/Contents/Resources/
        cp packaging/macos/Info.plist bundle/libresprite.app/Contents/
        chmod +x bundle/libresprite.app/Contents/MacOS/libresprite
    - name: Put in Dylibs
      run: |
        dylibbundler -od -b -ns -x bundle/libresprite.app/Contents/MacOS/libresprite -d bundle/libresprite.app/Contents/libs/
        echo "Manually bundling libSDL3.dylib..."
        # Homebrew's sdl2 formula now installs sdl2-compat, whose libSDL2 dylib
        # dlopen()s SDL3 at runtime instead of linking it (checks @loader_path,
        # @executable_path, then plain name). dylibbundler only reads LC_LOAD_DYLIB
        # entries via otool, so it never sees this dependency and skips it.
        cp -L "$(brew --prefix sdl3)/lib/libSDL3.dylib" bundle/libresprite.app/Contents/libs/libSDL3.dylib
        install_name_tool -id "@rpath/libSDL3.dylib" bundle/libresprite.app/Contents/libs/libSDL3.dylib
        echo "RPATH entries after dylibbundler:"
        otool -l bundle/libresprite.app/Contents/MacOS/libresprite | grep -A2 LC_RPATH || echo "No RPATH entries found"
        echo "Cleaning up RPATH entries from main executable..."
        otool -l bundle/libresprite.app/Contents/MacOS/libresprite | grep -A2 LC_RPATH | grep path | awk '{print $2}' | sort | uniq | while read -r rpath; do
            if [ ! -z "$rpath" ]; then
                echo "Removing RPATH: $rpath"
                install_name_tool -delete_rpath "$rpath" bundle/libresprite.app/Contents/MacOS/libresprite 2>/dev/null || echo "Could not remove $rpath (may not exist)"
            fi
        done
        echo "Adding single RPATH: @executable_path/../libs/"
        install_name_tool -add_rpath "@executable_path/../libs/" bundle/libresprite.app/Contents/MacOS/libresprite 2>/dev/null || echo "RPATH may already exist"
        echo "Final RPATH entries:"
        otool -l bundle/libresprite.app/Contents/MacOS/libresprite | grep -A2 LC_RPATH || echo "No RPATH entries found"
        echo "Cleaning RPATH entries from all bundled libraries:"
        find bundle/libresprite.app/Contents/libs -name "*.dylib" -type f | while read -r dylib; do
            echo "Processing: $(basename "$dylib")"
            otool -l "$dylib" | grep -A2 LC_RPATH | grep path | awk '{print $2}' | sort | uniq | while read -r rpath; do
                if [ ! -z "$rpath" ]; then
                    echo "  Removing RPATH: $rpath"
                    install_name_tool -delete_rpath "$rpath" "$dylib" 2>/dev/null || echo "  Could not remove $rpath from $(basename "$dylib")"
                fi
            done
        done
        echo "RPATH cleanup complete"
    - name: Sign the Binaries
      run: |
        # Remove all existing signatures before re-signing
        find bundle/libresprite.app -type f \( -name "*.dylib" -o -name "libresprite" \) -exec codesign --remove-signature {} \; 2>/dev/null || true

        # Ad-hoc sign without hardened runtime (--options=runtime triggers CS_RUNTIME which
        # makes recent macOS releases enforce strict page-level validation on every loaded
        # dylib — validation that ad-hoc signatures produced by older runners fail. Without
        # CS_RUNTIME, library validation is not kernel-enforced and all dylibs load freely.)
        find bundle/libresprite.app/Contents/libs -name "*.dylib" | while read -r dylib; do
          echo "Signing: $dylib"
          codesign --force -s - "$dylib" && echo "✓ $dylib" || echo "✗ $dylib"
        done

        codesign --force -s - bundle/libresprite.app/Contents/MacOS/libresprite && \
          echo "LibreSprite contents signed successfully" || echo "Failed to sign LibreSprite contents"

        codesign --force -s - bundle/libresprite.app && \
          echo "LibreSprite app signed successfully" || echo "Failed to sign LibreSprite app"
    - name: Create the DMG
      run: |
        dmgbuild --detach-retries 5 -Dapp=bundle/libresprite.app -s packaging/macos/dmg_settings.py "LibreSprite" libresprite.dmg
        if [ "${{ steps.check_tag.outputs.has_tag }}" = "true" ]; then
          mv libresprite.dmg "libresprite-${{ steps.check_tag.outputs.tag }}-macos-arm64.dmg"
          echo "dmg=libresprite-${{ steps.check_tag.outputs.tag }}-macos-arm64.dmg" >> $GITHUB_OUTPUT
        else
          echo "dmg=libresprite.dmg" >> $GITHUB_OUTPUT
        fi
      id: dmg
    - name: Upload Artifacts
      uses: actions/upload-artifact@v7
      with:
        name: ${{ steps.check_tag.outputs.has_tag == 'true' && format('libresprite-{0}-macos-arm64', steps.check_tag.outputs.tag) || 'libresprite-macos-arm64' }}
        path: ${{ steps.dmg.outputs.dmg }}
        if-no-files-found: error
    - name: Attach the DMG to the release
      if: steps.check_tag.outputs.has_tag == 'true'
      uses: softprops/action-gh-release@v3
      with:
        files: ${{ steps.dmg.outputs.dmg }}
        fail_on_unmatched_files: true

  build-x86_64:
    runs-on: macos-15-intel
    if: ${{ !contains(github.event.head_commit.message, 'NO_SW_CHANGE') && !contains(github.event.pull_request.body, 'NO_SW_CHANGE') }}
    steps:
    - uses: actions/checkout@v7
      with:
        submodules: 'true'
    - name: Check for git tag
      id: check_tag
      run: |
        TAG=$(git describe --exact-match --tags 2>/dev/null) || true
        if [ -n "$TAG" ]; then
          echo "has_tag=true" >> $GITHUB_OUTPUT
          echo "tag=$TAG" >> $GITHUB_OUTPUT
          echo "version=${TAG#v}" >> $GITHUB_OUTPUT
        else
          echo "has_tag=false" >> $GITHUB_OUTPUT
        fi
    - name: Get short commit hash
      id: commit
      run: |
        SHORT_HASH=$(git rev-parse --short HEAD)
        echo "short_hash=$SHORT_HASH" >> $GITHUB_OUTPUT
    - name: Prepare release version
      run: |
        sed -i '' "s/\"local build\"/\"${{ steps.commit.outputs.short_hash }}\"/g" src/config.h
        if [ "${{ steps.check_tag.outputs.has_tag }}" = "true" ]; then
          VER="${{ steps.check_tag.outputs.version }}"
          /usr/libexec/PlistBuddy -c "Set :CFBundleVersion $VER" packaging/macos/Info.plist
          /usr/libexec/PlistBuddy -c "Set :CFBundleShortVersionString $VER" packaging/macos/Info.plist
          /usr/libexec/PlistBuddy -c "Set :CFBundleLongVersionString $VER" packaging/macos/Info.plist
        fi
    - name: Prepare environment and install dependencies
      run: |
        # macos-latest runner images come with this tap pre-added; Homebrew's
        # tap-trust check then warns on every brew invocation since it's untrusted.
        brew untap aws/tap || true
        brew install autoconf automake libtool gnutls freetype jpeg webp pixman sdl2 sdl2_image tinyxml2 libarchive ninja zlib xmlto dylibbundler
        pip3 install --break-system-packages dmgbuild
        echo "Building and Installing GifLib"
        export CFLAGS="-Wno-implicit-function-declaration"
        curl -L https://sourceforge.net/projects/giflib/files/giflib-4.x/giflib-4.2.3.tar.gz/download --output giflib.tar.gz
        tar -xvf giflib.tar.gz
        cd giflib-4.2.3/
        ./configure
        sudo make install -j4
        cd ..
        echo "Building and installing LibPNG"
        git clone https://github.com/glennrp/libpng -b v1.6.50 --recursive
        cd libpng/
        ./configure
        make all -j4
        sudo make install
        cd ..
        plutil packaging/macos/Info.plist
    - name: Generate Makefiles
      run: |
        mkdir build
        if [ "${{ steps.check_tag.outputs.has_tag }}" = "true" ]; then
          cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release \
            -DRELEASE_TAG=ON \
            -DRELEASE_VERSION="${{ steps.check_tag.outputs.version }}" \
            -DCMAKE_OSX_ARCHITECTURES=x86_64 \
            -DCMAKE_OSX_DEPLOYMENT_TARGET=13.3 \
            -DCMAKE_OSX_SYSROOT=/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk
        else
          cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_OSX_ARCHITECTURES=x86_64 \
            -DCMAKE_OSX_DEPLOYMENT_TARGET=13.3 \
            -DCMAKE_OSX_SYSROOT=/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk
        fi
    - name: Build the binaries
      run: |
        # Build only the editor target so test/tool binaries (clip_*_tests,
        # gen) are never produced and can't leak into the bundle.
        cmake --build build -j 3 --target libresprite
        rm -f build/bin/gen
    - name: Generate .icns file
      run: |
        mkdir libresprite.iconset
        sips -z 16 16     data/icons/ase64.png --out libresprite.iconset/icon_16x16.png
        sips -z 32 32     data/icons/ase64.png --out libresprite.iconset/icon_16x16@2x.png
        sips -z 32 32     data/icons/ase64.png --out libresprite.iconset/icon_32x32.png
        sips -z 64 64     data/icons/ase64.png --out libresprite.iconset/icon_32x32@2x.png
        sips -z 128 128   data/icons/ase64.png --out libresprite.iconset/icon_128x128.png
        sips -z 256 256   data/icons/ase64.png --out libresprite.iconset/icon_128x128@2x.png
        sips -z 256 256   data/icons/ase64.png --out libresprite.iconset/icon_256x256.png
        sips -z 512 512   data/icons/ase64.png --out libresprite.iconset/icon_256x256@2x.png
        sips -z 512 512   data/icons/ase64.png --out libresprite.iconset/icon_512x512.png
        sips -z 1024 1024 data/icons/ase64.png --out libresprite.iconset/icon_512x512@2x.png
        iconutil -c icns -o libresprite.icns libresprite.iconset
        rm -R libresprite.iconset
    - name: Create Apple Bundle
      run: |
        mkdir bundle/
        mkdir bundle/libresprite.app
        mkdir bundle/libresprite.app/Contents
        mkdir bundle/libresprite.app/Contents/libs
        mkdir bundle/libresprite.app/Contents/MacOS
        mkdir bundle/libresprite.app/Contents/Resources
        cp build/bin/libresprite bundle/libresprite.app/Contents/MacOS/
        cp -r build/bin/data/ bundle/libresprite.app/Contents/Resources/data/
        cp libresprite.icns bundle/libresprite.app/Contents/Resources/
        cp packaging/macos/Info.plist bundle/libresprite.app/Contents/
        chmod +x bundle/libresprite.app/Contents/MacOS/libresprite
    - name: Put in Dylibs
      run: |
        dylibbundler -od -b -ns -x bundle/libresprite.app/Contents/MacOS/libresprite -d bundle/libresprite.app/Contents/libs/
        echo "Manually bundling libSDL3.dylib..."
        # Homebrew's sdl2 formula now installs sdl2-compat, whose libSDL2 dylib
        # dlopen()s SDL3 at runtime instead of linking it (checks @loader_path,
        # @executable_path, then plain name). dylibbundler only reads LC_LOAD_DYLIB
        # entries via otool, so it never sees this dependency and skips it.
        cp -L "$(brew --prefix sdl3)/lib/libSDL3.dylib" bundle/libresprite.app/Contents/libs/libSDL3.dylib
        install_name_tool -id "@rpath/libSDL3.dylib" bundle/libresprite.app/Contents/libs/libSDL3.dylib
        echo "RPATH entries after dylibbundler:"
        otool -l bundle/libresprite.app/Contents/MacOS/libresprite | grep -A2 LC_RPATH || echo "No RPATH entries found"
        echo "Cleaning up RPATH entries from main executable..."
        otool -l bundle/libresprite.app/Contents/MacOS/libresprite | grep -A2 LC_RPATH | grep path | awk '{print $2}' | sort | uniq | while read -r rpath; do
            if [ ! -z "$rpath" ]; then
                echo "Removing RPATH: $rpath"
                install_name_tool -delete_rpath "$rpath" bundle/libresprite.app/Contents/MacOS/libresprite 2>/dev/null || echo "Could not remove $rpath (may not exist)"
            fi
        done
        echo "Adding single RPATH: @executable_path/../libs/"
        install_name_tool -add_rpath "@executable_path/../libs/" bundle/libresprite.app/Contents/MacOS/libresprite 2>/dev/null || echo "RPATH may already exist"
        echo "Final RPATH entries:"
        otool -l bundle/libresprite.app/Contents/MacOS/libresprite | grep -A2 LC_RPATH || echo "No RPATH entries found"
        echo "Cleaning RPATH entries from all bundled libraries:"
        find bundle/libresprite.app/Contents/libs -name "*.dylib" -type f | while read -r dylib; do
            echo "Processing: $(basename "$dylib")"
            otool -l "$dylib" | grep -A2 LC_RPATH | grep path | awk '{print $2}' | sort | uniq | while read -r rpath; do
                if [ ! -z "$rpath" ]; then
                    echo "  Removing RPATH: $rpath"
                    install_name_tool -delete_rpath "$rpath" "$dylib" 2>/dev/null || echo "  Could not remove $rpath from $(basename "$dylib")"
                fi
            done
        done
        echo "RPATH cleanup complete"
    - name: Sign the Binaries
      run: |
        # Remove all existing signatures before re-signing
        find bundle/libresprite.app -type f \( -name "*.dylib" -o -name "libresprite" \) -exec codesign --remove-signature {} \; 2>/dev/null || true

        # Ad-hoc sign without hardened runtime (--options=runtime triggers CS_RUNTIME which
        # makes recent macOS releases enforce strict page-level validation on every loaded
        # dylib — validation that ad-hoc signatures produced by older runners fail. Without
        # CS_RUNTIME, library validation is not kernel-enforced and all dylibs load freely.)
        find bundle/libresprite.app/Contents/libs -name "*.dylib" | while read -r dylib; do
          echo "Signing: $dylib"
          codesign --force -s - "$dylib" && echo "✓ $dylib" || echo "✗ $dylib"
        done

        codesign --force -s - bundle/libresprite.app/Contents/MacOS/libresprite && \
          echo "LibreSprite contents signed successfully" || echo "Failed to sign LibreSprite contents"

        codesign --force -s - bundle/libresprite.app && \
          echo "LibreSprite app signed successfully" || echo "Failed to sign LibreSprite app"
    - name: Create the DMG
      run: |
        dmgbuild --detach-retries 5 -Dapp=bundle/libresprite.app -s packaging/macos/dmg_settings.py "LibreSprite" libresprite.dmg
        if [ "${{ steps.check_tag.outputs.has_tag }}" = "true" ]; then
          mv libresprite.dmg "libresprite-${{ steps.check_tag.outputs.tag }}-macos-x86_64.dmg"
          echo "dmg=libresprite-${{ steps.check_tag.outputs.tag }}-macos-x86_64.dmg" >> $GITHUB_OUTPUT
        else
          echo "dmg=libresprite.dmg" >> $GITHUB_OUTPUT
        fi
      id: dmg
    - name: Upload Artifacts
      uses: actions/upload-artifact@v7
      with:
        name: ${{ steps.check_tag.outputs.has_tag == 'true' && format('libresprite-{0}-macos-x86_64', steps.check_tag.outputs.tag) || 'libresprite-macos-x86_64' }}
        path: ${{ steps.dmg.outputs.dmg }}
        if-no-files-found: error
    - name: Attach the DMG to the release
      if: steps.check_tag.outputs.has_tag == 'true'
      uses: softprops/action-gh-release@v3
      with:
        files: ${{ steps.dmg.outputs.dmg }}
        fail_on_unmatched_files: true
